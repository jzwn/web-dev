The <pre> tag in HTML is used to display preformatted text. This tag helps to keep all the spaces, tabs, and line breaks exactly as we write them in the code. 
This is different from normal text where extra spaces or tabs may be ignored by the browser.
